I built a **v13 patch set** on top of your current v12 scripts.

Download it here: [rice-v13.zip](sandbox:/mnt/data/rice-v13.zip)

This directly fixes the problems you listed:

* Clock time reduced from the v12 `88px` size to `58px`, so it is about 66% of the old size. Your v12 clock also faded in/out with `280ms/200ms` and was positioned around 28% down the screen; v13 changes that to **18px from the top**, **650ms fade-in**, and **instant hide**. 
* Window opacity no longer fades during app switching. The v12 opacity extension used a smooth opacity function over about `200ms`; v13 applies opacity immediately. 
* Dock script no longer crashes on missing Dash-to-Panel keys like `leftBox-padding`; v13 uses safe `gsettings` setters and moves back to a centred 74% glass pill instead of styling the wrong empty centre container. 
* Super key and Arch icon are now separated properly:

  * **Super / Arch icon** → Start Menu
  * **Super+S** → app search only
* Added a **five-mode custom power system**:

  * Turbo
  * Performance
  * Normal
  * PowerSaving
  * Ultra-PowerSaving
* Added a **GNOME Quick Settings power-mode entry** for Super+A. GNOME extensions officially support adding Quick Settings toggles/menus through `QuickSettings.SystemIndicator` and `QuickSettings.QuickMenuToggle`, which is the route v13 uses. ([GJS Guide][1])
* The native Linux `power-profiles-daemon` only exposes the standard profile layer through D-Bus, so v13 maps your five custom modes onto the available native profiles while adding extra UI, brightness, wallpaper, and animation behaviour on top. Arch’s package description confirms `power-profiles-daemon` provides power profile handling over D-Bus, and `powerprofilesctl` works with profiles such as `performance` and `power-saver`. ([Arch Linux][2])

Run it like this:

```bash
mkdir -p ~/rice-v13 && unzip -o ~/Downloads/rice-v13.zip -d ~ && chmod +x ~/rice-v13/*.sh && bash ~/rice-v13/run-v13.sh
```

Then log out and log back in, because you are on Wayland and GNOME Shell extensions need a clean session reload.

After logging back in, run:

```bash
cat ~/rice-v13-status.txt
```

One honest limitation: I added power modes to **Quick Settings** and as an **IB Power Modes** settings-category app, but putting a fully custom panel *inside the GNOME Settings left sidebar* is not cleanly possible from a normal user script without patching/rebuilding `gnome-control-center`. The accessible route is now Super+A, Super+P, Start Menu → Power Modes, and app search → IB Power Modes.

[1]: https://gjs.guide/extensions/topics/quick-settings.html "Quick Settings | GNOME JavaScript"
[2]: https://archlinux.org/packages/extra/x86_64/power-profiles-daemon/?utm_source=chatgpt.com "power-profiles-daemon 0.30-1 (x86_64)"
